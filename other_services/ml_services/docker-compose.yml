services:
  validation:
    build: .
    volumes:
      - .:/app 
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - kafka-pipeline
    restart: always

networks:
  kafka-pipeline:
    external: true
    name: kafka_cluster_kafka-net